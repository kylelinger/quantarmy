# 2026-03-12 — QuantArmy 开发日志

## 项目状态
- **Version**: 0.1.0-dev
- **Repo**: https://github.com/kylelinger/quantarmy
- **Vercel**: https://frontend-beige-kappa-51.vercel.app
- **Project path**: /Users/kylex/clawd/quantarmy/

## 完成事项

### TradingView K线图修复 (3轮迭代)
1. 初始方案: script注入 `embed-widget-advanced-chart.js` → Vercel CSP阻断，空白
2. 第二次: iframe embed `s.tradingview.com/widgetembed/` → iframe渲染但内部空白(URL参数格式不对)
3. **最终方案**: `tv.js` + `TradingView.widget()` JS API → ✅ 完美工作
   - Commit: `2d58db7`
   - 组件: `frontend/components/Market/TradingViewChart.tsx`
   - 使用动态container + unique ID per mount
   - Studies: MASimple + Volume

### 侧边栏 + 自选列表 UX 修复
- "📋 自选标的" 从底部移到侧边栏最顶部 (权重最高的功能)
- 自选列表卡片: 整张卡片点击 → 直接导航到symbol详情页
- "分析"/"展开▾"/"删除✕" 按钮用 `e.preventDefault()` 独立于卡片导航
- Commit: `57678c5`

### Vercel部署注意事项
- Git push 不自动触发Vercel部署 (需手动 `vercel --prod --yes`)
- 每次改代码后: `npm run build` → `git commit` → `vercel --prod --yes`
- 部署约35-45秒完成

## Git Commits (今日)
- `0f4caa5` fix: switch TradingView to iframe embed (中间方案，已被替代)
- `2d58db7` fix: TradingView chart — switch to tv.js widget API (最终方案)
- `57678c5` fix: watchlist UX — sidebar top placement + card click navigates

## 关键文件
- `frontend/components/Market/TradingViewChart.tsx` — TradingView K线组件 (tv.js widget API)
- `frontend/components/Sidebar/SidebarConnected.tsx` — 侧边栏 (自选标的在顶部)
- `frontend/app/company/watchlist/page.tsx` — 自选列表页 (卡片点击导航)
- `frontend/app/company/watchlist/[symbol]/page.tsx` — Symbol详情页 (K线+8角色)

### 搜索自动补全 + 下拉 (19:00 IST)
- 替换盲输入为全搜索下拉 autocomplete
- 38 crypto + 34 stocks, 支持按 ticker/英文名/中文名搜索
- 搜索结果: 已添加→导航详情, 未添加→添加+导航
- 键盘 ↑↓ 导航, Enter 选择, Esc 关闭
- Commit: `3175101`

### 星星自选系统 + 移除停止交易按钮 (19:25 IST)
- 卡片右侧: ✕删除 → ⭐金色星星 (点击取消自选)
- 搜索下拉: 星星图标显示自选状态
- Symbol详情页: 标题旁 ⭐ 按钮, 添加/取消自选
- 未自选标的也能看K线图, 右侧提示"添加到自选"
- 侧边栏: 移除"停止交易"按钮及相关代码 (startTrading/stopTrading imports)
- Commit: `9d7b001`

## Git Commits (今日, 完整)
- `0f4caa5` fix: switch TradingView to iframe embed
- `2d58db7` fix: TradingView chart — switch to tv.js widget API
- `57678c5` fix: watchlist UX — sidebar top placement + card click navigates
- `3175101` feat: symbol search with autocomplete dropdown
- `9d7b001` feat: star-based watchlist toggle + remove trading button

## Session 3 (~19:00-19:56 IST)

### 完成
- **热门推荐优化**: 从12个缩减到5个，统一星星逻辑 (commit `daa34de`)
- **卡片清理**: 移除priority emoji图标 (commit `c3bc283`)，移除notes描述文字 (commit `ec1e89d`)
- **模拟盘 Dashboard 大改造** (commit `007696d`) — 公司概览页变为完整纸盘交易中心:
  - `frontend/lib/paper-trading.ts` — 客户端纸盘引擎，localStorage持久化
  - $100K模拟账户，0.1%手续费模型
  - 下单弹窗: 自选标的选择、做多/做空、快捷金额、止损止盈、下单理由
  - 平仓弹窗: 输入平仓价，实时预估盈亏
  - 调仓弹窗: 修改止损/止盈
  - 仪表盘: 总权益、盈亏、可用资金、持仓占比、胜率
  - 权益曲线: lightweight-charts渲染，每次交易更新
  - 三Tab: 当前持仓 / 交易记录 / 团队状态
  - 账户重置功能
  - API routes改为占位符(数据全在客户端)

### 关键文件
- `frontend/lib/paper-trading.ts` — 纸盘引擎核心 (PaperAccount, openPosition, closePosition, adjustPosition, updatePrices, resetAccount)
- `frontend/app/company/page.tsx` — 模拟盘Dashboard (OrderModal, CloseModal, AdjustModal, PositionsPanel, HistoryPanel, TeamPanel)

### 架构决策
- 纸盘数据全部客户端管理 (Next.js API routes是server-side，无法访问localStorage)
- API routes保留为空壳，未来对接真后端时替换
- paper-trading.ts 是独立模块，不依赖任何API

## 待办
- [ ] 后端部署到Render (via web dashboard)
- [ ] 前端API对接真后端 (目前demo API routes)
- [x] ~~公司概览dashboard页面~~ → 已改造为模拟盘
- [ ] V2 battle/debate 系统设计
- [ ] Landing page 改进
- [ ] WebSocket 实时推送
- [ ] 模拟下单与团队分析联动 (从详情页一键下单)
- [ ] 实时价格更新 (updatePrices接口已备好)

## Session 4 (~19:56 IST) — 全项目Review

### CEO要求全项目review，我给出了11条优化建议，按优先级排列：

#### 🔴 高优先级
1. `company/page.tsx` 720行太重 → 拆成 Trading/ 组件目录
2. 下单弹窗没有自动填入当前市价 → 选标的后调ticker API
3. 详情页→模拟盘打通断裂 → 详情页加"一键下单"按钮
4. 权益曲线新账户生成随机波动 → 应该是$100K水平线

#### 🟡 中优先级
5. 缺Toast通知系统 → 只有error toast，缺success/info
6. Watchlist(API route) vs PaperTrading(localStorage) 数据隔离 → 需统一
7. 移动端适配缺失 → sidebar 280px固定，手机不可用
8. TradeLog永远"未连接" → Vercel上ws://localhost永远连不上

#### 🟢 低优先级
9. demo-store.ts硬编码分析数据
10. hooks.ts到处any类型
11. formatCurrency重复逻辑

### 建议执行顺序
1. 详情页加"一键下单" (体验提升最大)
2. 下单弹窗自动填市价 (可用性硬伤)
3. 修复权益曲线初始值 (小改大影响)
4. 拆分page.tsx大文件 (可维护性)
5. TradeLog改用paper-trading数据 (消除"未连接")

### CEO批准全部5个 → 按顺序执行完毕

## Session 5 (~20:00 IST) — 5大优化全部落地

### 已完成 (commit `bf31851`)

1. **详情页一键下单** — symbol detail页右上角绿色"📝 一键下单"按钮
   - 展开后: 做多/做空切换 + $1K/$5K/$10K快捷金额 + 自定义输入
   - 自动获取Binance实时价格，直接在详情页下单不用跳转

2. **下单弹窗自动填市价** — OrderModal选完标的自动调 `/api/market/ticker24h` 填入价格
   - 新增"🔄 刷新市价"按钮

3. **权益曲线初始值修复** — `generateInitialEquityCurve()` 改为只返回单点 $100K
   - 新账户不再显示假随机波动

4. **代码拆分** — 720行page.tsx → 主文件约250行 + 7个组件
   ```
   components/Trading/
     OrderModal.tsx, CloseModal.tsx, AdjustModal.tsx
     PositionsPanel.tsx, HistoryPanel.tsx, TeamPanel.tsx
     MetricCard.tsx, Toast.tsx
   ```

5. **底部日志改造** — `TradeLogConnected` (WebSocket, 永远"未连接") → `PaperTradeLog` (读localStorage)
   - 显示"📜 交易日志 · 模拟盘"，可折叠，2s轮询刷新

6. **Toast通知系统** — `ToastProvider` + `useToast()` hook
   - success/error/info三种类型，3.5s自动消失
   - 包裹在 CompanyLayout 中

### 架构变更
- `frontend/app/company/layout.tsx` — TradeLogConnected → PaperTradeLog + ToastProvider包裹
- `frontend/components/TradeLog/PaperTradeLog.tsx` — 新文件，读paper-trading数据
- `frontend/components/Trading/Toast.tsx` — Context-based toast系统
- `frontend/app/company/watchlist/[symbol]/page.tsx` — 新增快速下单栏 + openPosition import

### Git
- Commit: `bf31851` feat: 5 optimizations
- Deployed to Vercel: https://frontend-beige-kappa-51.vercel.app
- Pushed to GitHub: https://github.com/kylelinger/quantarmy

### 剩余待办
- [ ] 后端部署到Render
- [ ] 前端API对接真后端
- [ ] V2 battle/debate 系统设计
- [ ] Landing page 改进
- [ ] 移动端适配 (sidebar固定280px，手机不可用)
- [ ] Watchlist数据源统一 (API route vs localStorage)
- [ ] demo-store.ts硬编码分析数据替换
- [ ] hooks.ts类型补全 (去any)

## Session 6 (~20:10-20:22 IST) — 小修 + 大规划

### 小修已完成
- `793b79b` fix: OrderModal下拉去掉重复的BTCUSDT/ETHUSDT硬编码选项
- `edf9a88` fix: 侧边栏"公司概览" → "仪表盘"

### CEO要求: 大幅加强8角色能力
- 现状: 全部硬编码假数据 (demo-store.ts)
- 目标: 基于真实市场数据实时计算分析结果

### 8角色能力规划 (已与CEO确认)
1. **Collector** — 24h量价 + 买卖盘深度比 + 大单监控 + 资金流向
2. **Strategist** — PSAR + EMA + ADX + RSI + MACD → 综合LONG/SHORT/HOLD
3. **Risk Officer** — ATR波动率 + 建议仓位 + 动态止损 + 风险评分 + 组合风险
4. **Analyst** — 支撑/阻力 + 趋势判定 + K线形态 + 量价分析 + 多时间框架共振
5. **Researcher** — 相似模式匹配 + 波动率分位 + 收益率分布 + β系数 + 周期效应
6. **Executor** — 买卖价差 + 滑点预估 + 流动性评分 + 执行策略建议
7. **CTO** — 数据新鲜度 + 完整度 + 异常检测
8. **CEO** — 综合判定 + 团队共识度 + 主要分歧 + 行动建议 + 失效条件

### 多市场支持 (CEO关键要求!)
CEO指出: "为什么只有Binance？股票至少要港股美股A股"

**数据源方案**:
| 市场 | 数据源 | 符号格式 |
|---|---|---|
| 加密货币 | Binance API | `BTCUSDT` |
| 美股 | Yahoo Finance | `AAPL` |
| 港股 | Yahoo Finance | `0700.HK` |
| A股 | Yahoo Finance | `600519.SS` / `000001.SZ` |

Yahoo Finance免费无API key，一个接口覆盖三市场。

### 执行计划 (CEO待批准)
1. Yahoo Finance API routes (klines + quote)
2. 统一数据适配器 (Binance/Yahoo自动切换)
3. 标的库扩充 (港股30+ / A股30+)
4. 搜索支持四个市场
5. 8角色分析引擎 (indicators.ts + 8个角色计算模块)
6. 详情页UI从假数据切到真计算

### 技术架构
```
新增API routes:
  /api/market/klines?symbol=X&interval=1h&limit=200
  /api/market/depth?symbol=X&limit=20
  /api/market/trades?symbol=X&limit=100

新增分析引擎:
  frontend/lib/analysis/
    indicators.ts, collector.ts, strategist.ts, risk-officer.ts,
    analyst.ts, researcher.ts, executor.ts, cto.ts, ceo.ts, types.ts
```

### 关键决策
- 统一适配器模式: 8角色不需要知道数据来自Binance还是Yahoo
- 股票市场报价有15min延迟(Yahoo免费版限制)，可接受
- 股票没有orderbook深度数据，Executor对股票只分析价差+成交量

---

## Session 7 (~20:30 IST) — 四市场 + 8角色分析引擎落地

### 已完成 (commit `b4966db`, +1830行)

1. **Yahoo Finance API routes** — `ticker24h`, `klines`, `depth`, `trades` 全部支持双数据源
   - `detectSource()` 自动路由: USDT/BTC后缀→Binance, 其余→Yahoo
   - 4h K线: Yahoo无原生4h，fetch 1h然后`aggregate4h()`聚合

2. **标的库扩充** — 四市场共 ~130只标的
   - 港股30只: 0700.HK(腾讯), 9988.HK(阿里), 1810.HK(小米), 1211.HK(比亚迪)...
   - A股30只: 600519.SS(茅台), 300750.SZ(宁德时代), 002594.SZ(比亚迪), 002230.SZ(科大讯飞)...
   - 搜索: 4个Tab（₿加密货币 🇺🇸美股 🇭🇰港股 🇨🇳A股），中文别名搜索

3. **TradingView symbol mapping** — 港股`HKEX:`, 上交所`SSE:`, 深交所`SZSE:`
   - NASDAQ_STOCKS Set扩充到28只常见NASDAQ股票

4. **技术指标库** (`frontend/lib/analysis/indicators.ts`, ~280行)
   - SMA, EMA, RSI, MACD, ATR, ADX, PSAR, Bollinger Bands
   - Support/Resistance (pivot-based swing detection)
   - Candlestick patterns (Doji, Hammer, Engulfing, Three Soldiers/Crows)
   - Correlation, percentile, daily returns

5. **8角色分析引擎** (`frontend/lib/analysis/`)
   - `collector.ts` — 量价分析, 买卖盘比, 大单监控, 资金流向
   - `strategist.ts` — PSAR(0.01/0.01/0.10)+EMA+ADX+RSI+MACD评分→信号
   - `risk-officer.ts` — ATR波动率, 仓位建议, 动态止损止盈, 近期最大回撤
   - `analyst.ts` — MA排列, 支撑/阻力, K线形态, 量能趋势, 多TF共振
   - `researcher.ts` — 波动率分位, 收益分布, BTC β, 周期效应, 历史形态匹配
   - `executor.ts` — 价差bps, 滑点预估($1K/$10K), 流动性评分, 执行策略建议
   - `cto.ts` — 数据新鲜度, 完整度, 异常检测, 角色间矛盾检查
   - `ceo.ts` — 多维共识汇总, LONG/SHORT/HOLD/WAIT判定, 行动方案, 失效条件
   - `index.ts` — 编排器: 并行fetch数据 → 串行跑8角色 → 返回FullAnalysis

### 关键架构决策
- **纯前端计算**: 所有技术指标和分析都在浏览器端JS运算，不需要后端
- **PSAR用慢速AF**: 0.01/0.01/0.10 (与AItrading production一致)
- **策略师评分制**: 各指标加权打分(PSAR 2分, EMA 2分, ADX调节, RSI 1分, MACD 1分)
- **CEO聚合逻辑**: 共识>0.3且风险≤7 → LONG; 共识<-0.3 → SHORT; 风险≥8 → WAIT
- **BTC β系数**: 只对非BTC的加密货币计算, 用日线收益率相关性

### 文件索引
- `frontend/lib/market-adapter.ts` — 统一市场数据适配器
- `frontend/lib/analysis/` — 8角色分析引擎目录
- `frontend/app/api/market/{klines,depth,trades,ticker24h}/route.ts` — API路由

### 下一步
- 详情页UI从demo-store假数据切换到真分析引擎
- 用户打开任意标的 → 自动运算8角色实时结果

---

## Session 8 (~20:45 IST) — 美股移除 + 新浪财经实时数据

### CEO决策
- **美股移除**: CEO不接受Yahoo Finance 15min延迟，且免费实时美股API都需要API key(如Finnhub)
- **优先覆盖**: 加密货币 + 港股 + A股 三个市场
- **Finnhub方案被否**: 虽然免费但需要注册API key，CEO选择先不加

### 已完成 (commit `9a14e49`)

1. **新浪财经API替换Yahoo Finance** — HK/A-shares实时报价
   - A股报价: `hq.sinajs.cn/list=sh600519` (GBK编码解析)
   - 港股报价: `hq.sinajs.cn/list=hk00700`
   - A股K线: `quotes.sina.cn/cn/api/jsonp_v2.php/.../CN_MarketDataService.getKLineData` (JSONP格式)
   - 港股K线: 仍用Yahoo Finance (历史K线不延迟，只是实时报价延迟)
   - 全部通过Next.js API route服务端代理，无CORS问题

2. **移除ALL_US_STOCKS** — 34只美股从标的库删除
   - MarketType: `'crypto' | 'hk_stock' | 'a_share'` (去掉us_stock)
   - 搜索Tab: 3个 (₿加密货币 / 🇭🇰港股 / 🇨🇳A股)

3. **TradingView映射简化** — 移除NASDAQ_STOCKS Set

### 数据源最终方案
| 市场 | 报价 | K线 | 深度/成交 |
|---|---|---|---|
| 加密货币 | Binance (实时) | Binance | Binance |
| 港股 | 新浪财经 (实时) | Yahoo Finance | ❌ |
| A股 | 新浪财经 (实时) | 新浪JSONP (实时) | ❌ |

### 新浪API关键细节
- **编码**: GBK，需`TextDecoder('gbk')`
- **A股字段顺序**: name,open,prevClose,price,high,low,bid,ask,volume,turnover...
- **港股字段顺序**: nameEn,nameCn,open,prevClose,high,low,price,change,changePct,...volume,turnover
- **A股K线JSONP响应**: `data([{day:"...",open:"...",high:"...",...}])` — 需正则提取JSON
- **Referer头必须**: `Referer: https://finance.sina.com.cn`
- **Sina K线最大**: datalen=1023

### Commit历史
- `b4966db` — 四市场 + 8角色分析引擎
- `9a14e49` — 移除美股, 新浪财经替换Yahoo
